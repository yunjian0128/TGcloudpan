{
  "name": "telegram-cloud-drive",
  "version": "1.0.0",
  "description": "Cloud storage system using Cloudflare Pages and Telegram Bot",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler pages dev dist --compatibility-date=2024-01-01",
    "build": "node build.js",
    "deploy": "wrangler pages deploy dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cloudflare", "pages", "telegram", "bot", "cloud-storage"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
